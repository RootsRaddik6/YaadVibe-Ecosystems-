import MapContainer from "../(ui)/map/MapContainer";

export default function MapPage() {
  return (
    <div style={{ padding: "24px" }}>
      <h1 style={{ fontSize: "24px", marginBottom: "16px" }}>
        Jamaica 3D Map (Coming Soon)
      </h1>

      <MapContainer />
    </div>
  );
}
